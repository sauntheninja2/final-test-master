version: 0.2

phases:
  build:
    commands:
      - echo Entered the build phase...
#       - mvn clean package
  post_build:
    commands:
      - echo Build completed successfully
      - aws cloudformation package --template-file template.yaml --s3-bucket codepipeline-ap-south-1-182844376226 --output-template-file packaged.yaml
artifacts:
  files:
    - target/cicdDemo.jar
    - template.yaml
  discard-paths: yes
  
  
